(ns think.cljs.test
	(:require [cemerick.cljs.test :as t]))


(defn run-tests
	[]
	; (t/test-ns 'think.cljs.test.model.document)
	; (t/test-ns 'think.cljs.test.model.document)
	; (t/test-ns 'think.cljs.test.model.document)
	; (t/test-ns 'think.cljs.test.model.document)
	(t/test-ns 'think.cljs.test.model))
